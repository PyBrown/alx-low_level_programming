#include "main.h"

/**
 * add - entry point
 * @x: number being added
 * @y: number being added
 *
 * Description: adds two integers and returns the result
 *
 * Return: 0
 */

int add(int x, int y)
{
	return (x + y);
}
